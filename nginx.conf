server {
 listen 80; server_name _; root /usr/share/nginx/html; index index.html;
 location /api/ { proxy_pass http://${BACKEND_PROXY_HOST}; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_read_timeout 300s; proxy_send_timeout 300s; }
 location /oauth2/ { proxy_pass http://${BACKEND_PROXY_HOST}; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
 location /login/oauth2/ { proxy_pass http://${BACKEND_PROXY_HOST}; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
 location /_ai/object-detection/ { proxy_pass http://${OBJECT_DETECTION_PROXY_HOST}/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_read_timeout 120s; }
 location / { try_files $uri $uri/ /index.html; }
}
